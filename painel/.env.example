# Endpoint do backend de atendimento. Em dev o default é http://localhost:3000;
# em build de produção o fallback é o Render (ver src/lib/config.js).
VITE_API_URL=http://localhost:3000

# Slug do tenant usado no login.
VITE_TENANT=summer
